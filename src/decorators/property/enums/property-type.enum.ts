export enum PropertyType {
  STRING = 'string',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  DATE = 'date',
  EMAIL = 'email',
  UUID = 'uuid',
  URL = 'url',
  JSON = 'json',
  ARRAY = 'array',
  OBJECT = 'object',
  ENUM = 'enum',
}
